#include<iostream>
#include<string>

int main(){
    double a,b;
    std::string op;
    std::cin>> a, op ,b;

    enum operation{  //da completare
        std::string p = "+",
    };


    switch (op)
    {
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }
}